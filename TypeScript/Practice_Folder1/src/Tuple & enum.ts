// Tuple: Fixed-length array with specific types

let info:[string,number]=["om",88]
console.log(info)

// Enum: Assigns readable names to values

enum role{manager,user,employee}
let position:role=role.user;
let position1:role=role.manager
